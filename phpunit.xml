<?xml version="1.0" encoding="UTF-8"?>
<!--
    Schema is 10.5. Coverage include/exclude therefore lives in <source>,
    not the 9.x <coverage> element, which now carries only report options.
    `beStrictAboutTodoAnnotatedTests` was removed in PHPUnit 10.

    This suite ran on PHPUnit 9.6 until wp_mock was dropped, not by choice:
    10up/wp_mock 1.1.1 declares "phpunit/phpunit": "^9.6" in its own require
    block and has no PHPUnit 10 branch, so asking for "^9.6|^10.0" alongside
    it always resolved to 9.6. Brain Monkey declares no PHPUnit constraint at
    all, which is what freed this.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="depends,defects"
         beStrictAboutOutputDuringTests="true"
         failOnRisky="true"
         failOnWarning="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory suffix=".php">src</directory>
        </include>
        <!--
            src/Admin is deliberately NOT excluded. It was, on the grounds that
            it is "admin render glue: markup, no logic worth unit-testing" —
            but Amber covers its whole src/Admin on this same tooling, so the
            exclusion was habit rather than necessity, and it was hiding real
            branches: the interval-clamping sanitize callback, the audit
            filter/pagination parsing, and every capability guard.

            The exclusion came out in the same commit that added
            tests/Unit/Admin/SettingsPageTest.php, so the Coveralls number
            moved up rather than down. Do not re-add it without deleting those
            tests too.
        -->
    </source>
</phpunit>
